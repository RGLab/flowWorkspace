\name{pData}
\alias{pData}
\title{it doesn't use metadata slot of GatingSet, instead it directly access the pData of flow data}
\description{
  it doesn't use metadata slot of GatingSet, instead it
  directly access the pData of flow data
}

