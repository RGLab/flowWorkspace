echo "removing exploded boost header source..."
if [ -e src/trimmed_boost_1_52_0 ]; then
	rm -rf src/trimmed_boost_1_52_0
fi
for f in ./config.*; do
	rm -f $f
done
for f in ./autom4*; do
	rm -rf $f
done
if [ -e src/Makevars ]; then
	rm -f src/Makevars
fi
